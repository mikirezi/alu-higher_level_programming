# python-inheritance

This project explores Python inheritance concepts: introspection with `dir()`, subclassing built-in types, `isinstance()` vs `type()`, and building a small geometry class hierarchy (`BaseGeometry` → `Rectangle` → `Square`) using validation and method overriding.
