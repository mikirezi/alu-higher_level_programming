# SQL_more_queries

This project covers MySQL users and privileges, table constraints (NOT NULL, DEFAULT, UNIQUE, AUTO_INCREMENT, PRIMARY KEY, FOREIGN KEY), and advanced querying with JOIN, LEFT JOIN, subqueries, GROUP BY, and aggregate functions across a TV shows database.
